[project]
name = "face-blockchain"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Yosuf20", email = "jyosuf6@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "bs4>=0.0.2",
    "insightface>=1.0.1",
    "opencv-python>=5.0.0.93",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
    "web3>=8.0.0",
]

[project.scripts]
face-blockchain = "face_blockchain:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
